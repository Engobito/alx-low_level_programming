#include "main.h"
#include <ctype.h>

/**
 * mul - the function that multi two integres
 *
 * @a: the intiger one.
 * @b: the integer two.
 *
 * Return: 1 if uppercase , 0 if lowercase.
*/

int mul(int a, int b)
{
	return (a * b);
}
